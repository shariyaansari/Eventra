name: 'Welcome New Contributors'

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: '🎉 Thanks for opening your first issue in this repo! We appreciate your contribution. If you need help, check our README or ask a maintainer.'
          pr-message: '🚀 Thanks for submitting your first pull request! A maintainer will review it soon. Welcome to the project!'
